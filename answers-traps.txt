1. a0,a1,a2 contain arguments
2.  26:	45b1      li	a1,12
    f 和 g 都是纯函数，即确定性且无副作用（不改变全局变量，IO， 文件，输出等，
    不依赖外部状态变化）
    被编译器常数传播 + 内联优化 constant folding + inlining 
3. printf 的地址在 1536(ra) # 630 <printf>
4. ra = 38 jalr ra, ra, 1536
5. Hell0 World, 这里 %x 表示 57616 作为一个整体整数传入，与大小端布局无关
    %s &i，传入的地址，从当前地址以字符串读取，直到 \0，因此考虑 0x00646c72 的布局
    大小端对应先写入高位还是低位，小端就是先将低位写入低地址
6. 垃圾值或者访问非法内存